# "loadhigh" gentoo portage overlay

To add this overlay run the following command:

```
layman -o https://raw.githubusercontent.com/loadhigh/loadhigh-overlay/master/repositories.xml -L -a loadhigh
```
